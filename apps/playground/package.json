{
  "name": "@formulax/playground",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "clean": "Remove-Item -Recurse -Force dist -ErrorAction SilentlyContinue",
    "dev": "vite"
  },
  "dependencies": {
    "@formulax/core": "workspace:*",
    "@formulax/editor": "workspace:*",
    "@formulax/renderer-katex": "workspace:*",
    "@formulax/ui": "workspace:*"
  }
}
